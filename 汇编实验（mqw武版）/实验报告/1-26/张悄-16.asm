DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    CALL GETENTER
    
    MOV AH,4CH
    INT 21H
    
    GETENTER PROC
    	PUSH AX
    	PUSH DX
    	
    	MOV DL,10
    	MOV AH,2
    	INT 21H
    	MOV DL,13
    	MOV AH,2
    	INT 21H
    	POP DX
    	POP AX
    	RET
    GETENTER ENDP
    
CODES ENDS
    END START
